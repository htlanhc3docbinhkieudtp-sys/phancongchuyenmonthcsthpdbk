{
  "entities": {
    "SchoolPlan": {
      "title": "School Plan",
      "description": "School academic plan including teaching assignments, configuration, classes, teachers and subject departments",
      "type": "object",
      "properties": {
        "config": { "type": "object", "description": "School configuration and reductions" },
        "departments": { "type": "array", "description": "List of subject departments" },
        "subjects": { "type": "array", "description": "List of school subjects" },
        "classes": { "type": "array", "description": "List of class groups with homeroom" },
        "teachers": { "type": "array", "description": "List of teachers with concurrent duties" },
        "assignments": { "type": "array", "description": "List of teaching assignments" },
        "lockedCells": { "type": "array", "description": "List of locked or excluded cells" },
        "updatedAt": { "type": "number", "description": "Last modified timestamp" },
        "lastUpdatedBy": { "type": "string", "description": "User identifier who saved the plan" },
        "hasSubcollections": { "type": "boolean", "description": "Flag indicating weekly schedules and timetables are partitioned into subcollections" }
      }
    },
    "WeeklyScheduleDoc": {
      "title": "Weekly Schedule",
      "description": "Weekly teaching schedule assignments partition",
      "type": "object",
      "properties": {
        "weekNumber": { "type": "number", "description": "Week number (1-36)" },
        "semester": { "type": "string", "description": "Academic semester (HK1 or HK2)" },
        "assignments": { "type": "array", "description": "List of weekly assignments for classes and subjects" },
        "updatedAt": { "type": "number", "description": "Last modified timestamp" }
      }
    },
    "WeeklyTimetableDoc": {
      "title": "Weekly Timetable",
      "description": "Weekly school timetable slot matrix partition",
      "type": "object",
      "properties": {
        "weekNumber": { "type": "number", "description": "Week number (1-36)" },
        "timetable": { "type": "object", "description": "Timetable object containing slot details" },
        "updatedAt": { "type": "number", "description": "Last modified timestamp" }
      }
    },
    "VisitorAnalyticsDoc": {
      "title": "Visitor Analytics",
      "description": "Persistent visitor counter and traffic analytics for the school website",
      "type": "object",
      "properties": {
        "totalVisits": { "type": "number", "description": "Total visits count" },
        "uniqueVisitors": { "type": "number", "description": "Unique visitors count" },
        "todayDate": { "type": "string", "description": "Today's date in YYYY-MM-DD format" },
        "todayVisits": { "type": "number", "description": "Number of visits today" },
        "yesterdayVisits": { "type": "number", "description": "Number of visits yesterday" },
        "thisMonthVisits": { "type": "number", "description": "Number of visits this month" },
        "lastVisitedAt": { "type": "number", "description": "Timestamp of the last visit" },
        "dailyHistory": { "type": "object", "description": "Daily visit breakdown for the last 30 days" }
      }
    }
  },
  "firestore": {
    "/school_plans/{planId}": {
      "schema": "SchoolPlan",
      "description": "Active school academic plans"
    },
    "/school_plans/{planId}/weekly_schedules/{weekId}": {
      "schema": "WeeklyScheduleDoc",
      "description": "Weekly schedule assignments partitioned to ensure under 1MB Firestore limit"
    },
    "/school_plans/{planId}/weekly_timetables/{weekId}": {
      "schema": "WeeklyTimetableDoc",
      "description": "Weekly timetable slots partitioned to ensure under 1MB Firestore limit"
    },
    "/site_analytics/{docId}": {
      "schema": "VisitorAnalyticsDoc",
      "description": "Website visitor counter and traffic analytics"
    }
  }
}
